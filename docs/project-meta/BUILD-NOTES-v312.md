SubnetOps v312 build notes

Main changes in this pass:
- Diagram workspace now opens as a two-pane workspace instead of three panes.
- Project layout hides the main stage navigation pane on /diagram so the diagram becomes a dedicated workspace.
- Diagram page now starts with the actual topology canvas visible.
- Added a compact legend strip with small network icons.
- Added left-side diagram controls for scope, overlay, labels, and link details.
- Notification dropdown now closes when clicking outside.
- Requirements stage root now stays blank until the user clicks a card in the left pane.

Validation:
- Changed TS/TSX files passed a TypeScript transpile-module syntax check.
