# Requirements Field Lifecycle Audit

This audit lists all 83 fields declared by the frontend `RequirementsProfile`. It is intentionally strict: a field is not considered healthy merely because it is displayed or appears in a report. It must be saved and either produce concrete engineering output or be explicitly marked review-only.

| Field key | Card / category | Expected lifecycle | Source-level status | Runtime verdict |
|---|---|---|---|---|
| planningFor | core | Must influence site/topology intent; siteCount must create/refresh persisted Site rows. | registry, materializer-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| projectPhase | core | Must be saved and explicitly mapped to design output or review-only evidence. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| environmentType | core | Must influence site/topology intent; siteCount must create/refresh persisted Site rows. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| complianceProfile | core | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| siteCount | core | Must influence site/topology intent; siteCount must create/refresh persisted Site rows. | registry, materializer-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| usersPerSite | core | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| internetModel | core | Must influence site/topology intent; siteCount must create/refresh persisted Site rows. | registry, materializer-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| serverPlacement | core | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| primaryGoal | core | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| guestWifi | core | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| voice | core | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| management | core | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| printers | core | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| iot | core | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| cameras | core | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| wireless | core | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| remoteAccess | core | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| dualIsp | core | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| cloudConnected | core | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| guestPolicy | security | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| voiceQos | security | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| managementAccess | security | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| wirelessModel | security | Must be saved and explicitly mapped to design output or review-only evidence. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| remoteAccessMethod | security | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| cloudProvider | cloud | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| cloudConnectivity | cloud | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| resilienceTarget | unknown | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| securityPosture | security | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| trustBoundaryModel | security | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| adminBoundary | security | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| identityModel | security | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| cloudIdentityBoundary | cloud | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| cloudTrafficBoundary | cloud | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| cloudHostingModel | cloud | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| cloudNetworkModel | cloud | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| cloudRoutingModel | cloud | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| addressHierarchyModel | addressing | Must influence addressing hierarchy, site blocks, gateway convention, capacity/headroom, naming/placement, or allocator notes. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| siteBlockStrategy | addressing | Must influence addressing hierarchy, site blocks, gateway convention, capacity/headroom, naming/placement, or allocator notes. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| gatewayConvention | addressing | Must influence addressing hierarchy, site blocks, gateway convention, capacity/headroom, naming/placement, or allocator notes. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| growthBufferModel | addressing | Must influence addressing hierarchy, site blocks, gateway convention, capacity/headroom, naming/placement, or allocator notes. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| reservedRangePolicy | addressing | Must influence addressing hierarchy, site blocks, gateway convention, capacity/headroom, naming/placement, or allocator notes. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| managementIpPolicy | addressing | Must influence addressing hierarchy, site blocks, gateway convention, capacity/headroom, naming/placement, or allocator notes. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| siteIdentityCapture | operations | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| namingStandard | operations | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| deviceNamingConvention | operations | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| namingTokenPreference | operations | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| namingHierarchy | operations | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| customNamingPattern | operations | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| monitoringModel | operations | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| loggingModel | operations | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| backupPolicy | operations | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| operationsOwnerModel | operations | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| siteLayoutModel | physical | Must influence addressing hierarchy, site blocks, gateway convention, capacity/headroom, naming/placement, or allocator notes. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| physicalScope | physical | Must influence addressing hierarchy, site blocks, gateway convention, capacity/headroom, naming/placement, or allocator notes. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| printerCount | physical | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| phoneCount | physical | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| apCount | physical | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| cameraCount | physical | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| serverCount | physical | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| iotDeviceCount | physical | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| wiredWirelessMix | physical | Must influence addressing hierarchy, site blocks, gateway convention, capacity/headroom, naming/placement, or allocator notes. | registry, materializer-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| siteRoleModel | physical | Must be saved and explicitly mapped to design output or review-only evidence. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| buildingCount | physical | Must influence addressing hierarchy, site blocks, gateway convention, capacity/headroom, naming/placement, or allocator notes. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| floorCount | physical | Must influence addressing hierarchy, site blocks, gateway convention, capacity/headroom, naming/placement, or allocator notes. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| closetModel | physical | Must influence addressing hierarchy, site blocks, gateway convention, capacity/headroom, naming/placement, or allocator notes. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| edgeFootprint | physical | Must influence addressing hierarchy, site blocks, gateway convention, capacity/headroom, naming/placement, or allocator notes. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| applicationProfile | appswan | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| criticalServicesModel | appswan | Must influence materialized VLAN/segment rows, host demand, service placement, security zone, or operations segment evidence. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| interSiteTrafficModel | appswan | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| bandwidthProfile | appswan | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| latencySensitivity | appswan | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| qosModel | appswan | Must influence security-flow, policy, WAN/cloud boundary, remote-access, QoS, or review evidence. | registry, materializer-ref, security-flow-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| outageTolerance | appswan | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, scenario-proof-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| growthHorizon | appswan | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| budgetModel | delivery | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| vendorPreference | delivery | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| implementationTimeline | delivery | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| rolloutModel | delivery | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| downtimeConstraint | delivery | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| teamCapability | delivery | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| outputPackage | delivery | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| primaryAudience | delivery | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |
| customRequirementsNotes | delivery | May be review/report/handoff evidence, but must be saved, visible, and explicitly classified if it does not create objects. | registry, materializer-ref, closure-ref | Runtime not proven in deployed UI; screenshots/report show downstream materialization is empty for the tested multi-site scenario. |

## Audit conclusion

The source model and registry both contain 83 fields, but runtime output proves that field capture does not equal field use. Phase 70 must instrument and fix the save/materialization path before deeper field-specific wiring can be trusted.
